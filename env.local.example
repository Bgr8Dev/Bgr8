# Frontend Email Configuration
# Copy this file to .env.local and update with your values

# Email API Configuration (Backend Server)
# For local development:
VITE_EMAIL_API_BASE_URL=http://localhost:3001

# For production deployment (optional - will auto-detect):
# VITE_EMAIL_API_BASE_URL=https://bgr8-email-server.onrender.com

# Your secure API key (get this from your email server)
VITE_EMAIL_API_KEY=your_secure_api_key_here

# NOTE: 
# - Zoho credentials are NOT stored in frontend for security
# - They should only be in the backend server's .env file
# - If VITE_EMAIL_API_BASE_URL is not set, it will automatically use:
#   - https://bgr8-email-server.onrender.com for production builds
#   - http://localhost:3001 for development

# ============================================
# Console Log Configuration
# ============================================
# Control console logging behavior across the application

# Master switch for all console logs (true/false or 1/0)
# Default: true in development, false in production
# VITE_CONSOLE_LOGS_ENABLED=true

# Enable console logs in development/local environment (true/false or 1/0)
# Default: true
# VITE_CONSOLE_LOGS_DEV=true

# Enable console logs in production environment (true/false or 1/0)
# Default: false
# VITE_CONSOLE_LOGS_PROD=false

# Enable analytics-related console logs (Google Analytics, tracking, etc.)
# Default: false
# VITE_CONSOLE_LOGS_ANALYTICS=false

# ============================================
# Firebase Server Configuration (Client-side)
# ============================================
# Firebase Server API URL for client-side requests
# For local development:
VITE_FIREBASE_SERVER_URL=http://localhost:4001

# For production deployment (optional - will auto-detect):
# VITE_FIREBASE_SERVER_URL=https://bgr8-firebase-server.onrender.com

# NOTE:
# - This URL is used by the client to connect to the firebase-server
# - If not set, automatically uses:
#   - https://bgr8-firebase-server.onrender.com for production builds
#   - http://localhost:4001 for development
# - The firebase-server provides secure Firestore operations via API

# ============================================
# Malware Scanning Configuration
# ============================================
# VirusTotal API Key for malware scanning on file uploads
# Get your free API key at: https://www.virustotal.com/gui/join-us
# Free tier allows up to 500 requests per day (hash lookups)
# For higher limits, consider upgrading to a paid plan

# VirusTotal API Key (required for malware scanning)
VITE_VIRUSTOTAL_API_KEY=your_virustotal_api_key_here

# NOTE:
# - Malware scanning is automatically enabled when API key is configured
# - Files are scanned before upload to Firebase Storage
# - If the API key is not set, scanning is disabled and uploads proceed normally
# - VirusTotal uses SHA-256 hash lookups by default (fast, efficient)
# - Files larger than 32MB cannot be scanned (VirusTotal limit)
