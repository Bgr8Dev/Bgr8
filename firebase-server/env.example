# Firebase Server Configuration
# Copy this file to .env in the firebase-server directory and update with your values

# Server Configuration
# Optional: Custom port (default: 4001)
FIREBASE_SERVER_PORT=4001

# Firebase Admin SDK Configuration (required)
# These credentials are used to authenticate server-side Firestore operations
# Get these from Firebase Console → Project Settings → Service Accounts → Generate New Private Key

# Your Firebase Project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# Service Account Email (from Firebase service account JSON)
FIREBASE_CLIENT_EMAIL=your-service-account@your-project-id.iam.gserviceaccount.com

# Service Account Private Key (from Firebase service account JSON)
# IMPORTANT: Keep the entire key on one line, with \n characters for newlines
# The key should start with "-----BEGIN PRIVATE KEY-----\n" and end with "\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# NOTE: 
# - These credentials should NEVER be committed to version control
# - Store them securely and use environment variables in production
# - The server uses the same Firebase Admin credentials as the Cal.com server
# - If you already have these set for the Cal.com server, you can reuse them
